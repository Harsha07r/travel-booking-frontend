// Mapping of tour titles to their MongoDB _id values
// Key MUST exactly match the `title` used in TourPage

const tourIds = {
  "5N-6D Kashmir Package": "63b2f8e1a1b2c3d4e5f6g7h8"
  // ↑ Replace this ID with your actual MongoDB tour _id if different
};

export default tourIds;
